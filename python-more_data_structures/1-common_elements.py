#!/usr/bin/env python3
def common_elements(set_1, set_2):
    # Use the intersection operation to find the common elements
    common_elements_set = set_1.intersection(set_2)
    return common_elements_set
